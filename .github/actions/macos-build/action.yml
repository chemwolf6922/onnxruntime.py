name: 'Build wheels for macOS'
description: 'Build wheels for macOS'
inputs:
  python-version:
    description: 'Python version to use'
    required: false
    default: '3.13'
runs:
  using: 'composite'
  steps:
    - name: Setup python
      uses: actions/setup-python@v4
      with:
        python-version: ${{ inputs.python-version }}

    - name: Install Python dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt delocate
      shell: bash

    - name: Build wheel
      run: |
        cd ${{ github.workspace }}
        rm -rf build dist
        mkdir -p build dist dist-final
        cmake -S . -B build -G Ninja -DCMAKE_BUILD_TYPE=Release
        cmake --build build
      shell: bash

    - name: Repair wheels with delocate
      run: |
        cd ${{ github.workspace }}
        for whl in dist/*.whl; do
          delocate-wheel -w dist-final -v "$whl" || cp "$whl" dist-final/
        done
      shell: bash
